def main(null<int8>){
    buf = Buffer(<4>, int8, __GLOBAL__);
    a = Load(buf, [0]);
    b = Load(buf, [1]);
    c = Load(buf, [2]);
    d = Load(buf, [3]);
    e = a == b;
    f = c == d;
    g = e && f;

    Print(g);
}