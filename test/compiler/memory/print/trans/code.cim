def main(null<int8>){
    buf1 = Buffer(<8>, index, local);
    Save(buf1, [0], 0);
    Save(buf1, [1], 1);
    Save(buf1, [2], 2);
    Save(buf1, [3], 3);
    subbuf1 = Slice(buf1, [0], [4], [1]);
    buf2 = Buffer(<8>, index, local);
    subbuf2 = Slice(buf2, [0], [4], [1]);
    Trans(subbuf1, subbuf2);
    e1 = Load(buf2, [0]);
    e2 = Load(buf2, [1]);
    e3 = Load(buf2, [2]);
    e4 = Load(buf2, [3]);
    Print(e1);
    Print(e2);
    Print(e3);
    Print(e4);
}