def main(null<int8>){
    buf1 = Buffer(<3,3>, index, local);
    Save(buf1, [0,0], 0);
    Save(buf1, [0,1], 1);
    Save(buf1, [0,2], 2);
    Save(buf1, [1,0], 3);
    Save(buf1, [1,1], 4);
    Save(buf1, [1,2], 5);
    Save(buf1, [2,0], 6);
    Save(buf1, [2,1], 7);
    Save(buf1, [2,2], 8);

    slice2 = Slice(buf1, [0,1], [3,2], [1,1]);
    Print(Load(slice2, [0,0])); // 1
    Print(Load(slice2, [0,1])); // 2
    Print(Load(slice2, [1,0])); // 4
    Print(Load(slice2, [1,1])); // 5
    Print(Load(slice2, [2,0])); // 7
    Print(Load(slice2, [2,1])); // 8

    slice3 = Slice(slice2, [1,0], [2,2], [1,1]);
    Print(Load(slice3, [0,0])); // 4
    Print(Load(slice3, [0,1])); // 5
    Print(Load(slice3, [1,0])); // 7
    Print(Load(slice3, [1,1])); // 8

    slice4 = Slice(slice3, [1,1], [1,1], [1,1]);
    Print(Load(slice4, [0,0])); // 8
}