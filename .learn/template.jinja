<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Jinja2 Example</title>
</head>
<body>
    <h1>Hello, {{ name }}!</h1>
    {% if age >= 18 %}
        <p>You are an adult.</p>
    {% else %}
        <p>You are not an adult.</p>
    {% endif %}
    <ul>
        {% for item in items %}
            <li>{{ item }}</li>
        {% endfor %}
    </ul>
</body>
</html>